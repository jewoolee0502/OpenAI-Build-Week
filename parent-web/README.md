# ImagineLab Parent Portal

The responsive React/Vite website for guardians. It shows a linked child's projects, activity,
playable previews, and project-specific AI observations.

```bash
npm install
cp .env.example .env
npm run dev
```

The backend must be running at `VITE_API_BASE_URL` (default: `http://localhost:8080`).
